%%

%%
function SimulateSequence(configs, burnSequence)

% Execute each burn in burnSequence
% In between each burn, numerically propagate the orbit
    % Propagation can be done either: Keplerian (geometrically, no integration), TwoBody (simple
    % physics model), or Encke (full pertubation model, numerical integration)



end